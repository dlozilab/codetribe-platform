import React from 'react'


// The ModuleList page will show all the modules available to the trainee. and a modal of module details when clicked on a module. 
// If the module details are too big then we will consider a page instead.
export default function ModuleList() {
  return (
    <div>moduleList</div>
  )
}
